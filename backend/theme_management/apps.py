from django.apps import AppConfig

class ThemeManagementConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'theme_management'
    verbose_name = 'Theme Management'
